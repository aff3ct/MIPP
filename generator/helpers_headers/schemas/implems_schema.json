{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ImplemsSchema",
  "anyOf": [
    {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": { "$ref": "#/definitions/item" }
      }
    },
    {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": {
          "type": "array",
          "items": { "$ref": "#/definitions/item" }
        }
      }
    }
  ],
  "definitions": {
    "item": {
      "type": "object",
      "properties": {
        "instr_name": { "type": "string" },
        "datatypes": {
          "anyOf": [
            { "type": "string" },
            { "type": "array", "items": { "type": "string" } }
          ]
        },
        "template": {
          "anyOf": [
            { "type": "string" },
            { "$ref": "#/definitions/embedded_template" }
          ]
        },
        "template_ref": { "type": "string" },
        "if": { "type": "string" },
        "level": { "type": "integer" },
        "version": { "type": "string", "enum": ["mask", "maskz", "masks", "horiz_lmul"] },
        "type": { "type": "string", "enum": ["native", "emulated"] },
        "dependencies": { "type": "array", "items": { "type": "string" } },
        "preferred_simd_ext": {
          "anyOf": [
            { "type": "string" },
            { "type": "array", "items": { "type": "string" } }
          ]
        },
        "simd_size": {
          "anyOf": [
            { "type": "integer" },
            { "type": "array", "items": { "type": "integer" } }
          ]
        }
      },
      "required": ["datatypes"],
      "anyOf": [
        { "required": ["template"] },
        { "required": ["template_ref"] }
      ]
    },
    "embedded_template": {
      "type": "object",
      "properties": {
        "format": { "type": "string", "enum": ["short", "long"] },
        "code": {
          "anyOf": [
            { "type": "string" },
            { "type": "array", "items": { "type": "string" } }
          ]
        }
      },
      "required": ["code"]
    }
  }
}
